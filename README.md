# CREATE FLASK 

### 1. Приложение
Данное приложение созданно мной в целях более удобного использования фреймворка Flask. 
Приложение создает базовую структуру проекта и создает пару файлов
Структура проекта выглядит так
```text
name/
├── app.py                # Основной файл приложения
├── templates/
│   └── index.html        # Главная страница
├── static/
│   ├── styles/
│   │   └── styles.css
│   └── js/
│       └── main.js       # Клиентская логика
│   └── fonts             # Шрифты
│   └── images            # Изображения
```
Из этого наполненными файлами являются токльо
```text
app.py
index.html
styles.css
```
## 2. Версии проекта
Текущая версия ```0.1```

Пока что проект находится на стадии разработки, не до конца доведена возможность создания venv для python. 
Также приложение работает с python не ниже 3.12.*! Проверено на MacOS macOS 15.6.1 24G90 arm64 и на Arch Linux.

# 3. Устновка и использование
Запустите installer.py
```bash
cd create_flask
python3 installer.py
```

После чего приложение Вас оповестит об успешной установке
А далее вы можете использовать
```bash
create-flask
```

Данная команда начнет создание проекта в текущей директории
У приложения есть аргументы, к примеру 
```bash
create-flask <name>
```

В данном случае вы получите созданную директорию с указанным именем. В обоих случаях Вас попросят ввести порт и <0/1> в зависимости от того, хотите ли вы использовать DebugMode
```python
app.run(port=<port>, debug=<debug(0/1)>)
```

Для более конкретной информации введите
```
create-flask --help
```
Вас оповестят о версии приложения, ее требования, а также где она была уже запущена и отработала штатно. Также вы увидите полный перечень аргументов доступных для использования с этим приложением.

## 4. Лицензия
Проект является открытым. Не предназначен для использования в коммерчиских целях! 
Буду рад если на этой основе будет созданно более привлекательное приложение. 
